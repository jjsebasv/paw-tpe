div.list-item.row.row-space-between(ng-click='goto({toType: "course", toId: id, fromName: from})')
  span
    | {{ code }} {{ name }}
  .row
    button.m-right-2
      img(src="images/edit.svg")
    button(ng-click='deleteCourse(id, $event)')
      img(src="images/remove.svg")
