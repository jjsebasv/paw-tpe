div(ng-controller='UniversityController as uniCtrl')
  .back(ng-click="appCtrl.goTo('^.home', null)")
    | < Volver
  button.bla(ng-click='indexCtrl.goto("/program/2")')
    | GILADA
  .column.column-center
    h1.title-3.text-base.green.m-bottom-3
      | {{uniCtrl.university.name}}
    searchbox(filter='searchText' placeholder='SEARCH_PROGRAM')
    .list-wrapper.column.column-center.list-wide
      program(name='program.name' id='program.programid' ng-repeat='program in uniCtrl.programs | filter:searchText')
